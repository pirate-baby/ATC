# ATC Development Environment Configuration
# Copy this file to .env and customize as needed
#
# Note: PORT_OFFSET and COMPOSE_PROJECT_NAME are automatically set by utils/startup.sh
# You typically don't need to set these manually.

# Port offset for multi-worktree support (0-5)
# Automatically calculated by startup script to avoid conflicts
# PORT_OFFSET=0

# Docker Compose project name (auto-set from branch name)
# COMPOSE_PROJECT_NAME=atc

# Database configuration (defaults match docker-compose.yml)
# DATABASE_URL=postgresql+psycopg2://atc:atc_dev@db:5432/atc

# Application environment
ENVIRONMENT=development

# JWT Configuration (required)
# In production, use a secure random secret (e.g., openssl rand -hex 32)
JWT_SECRET_KEY=atc-dev-jwt-secret-do-not-use-in-production

# Anthropic API Key (required for Claude-powered plan generation)
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=

# GitHub OAuth Configuration (required for login)
# Create a GitHub OAuth App at: https://github.com/settings/developers
# - Application name: ATC Local Development (or any name)
# - Homepage URL: http://localhost:80 (or http://localhost:3000)
# - Authorization callback URL: http://localhost:80/auth/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=http://localhost/auth/callback


REPOS_ROOT_PATH=./repos # Path to code repositories for analysis